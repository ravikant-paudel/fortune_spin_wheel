name: fortune_spin_wheel
description: A new Flutter project.
version: 1.0.0+1

environment:
  sdk: ">=2.16.1 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  quiver: ^3.0.1+1

dev_dependencies:
  flutter_test:
    sdk: flutter
